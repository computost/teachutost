{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "create-sln": {
      "inputs": ["../../{apps,packages}/**/*.*proj"],
      "outputs": ["../../Teachutost.sln"]
    },
    "dotnet-tool-restore": {},
    "install-chromium": { "cache": false, "interactive": true },
    "resharper": {
      "dependsOn": ["create-sln"],
      "inputs": ["../../**/*.cs"],
      "outputs": ["./bin/inspectcode.sarif"]
    }
  }
}
